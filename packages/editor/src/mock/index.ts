import form from "./form.json"
import page from "./page.json"
import { DEFAULT as formDefault } from "@tefact/feature-form";
import { DEFAULT as pageDefault } from "@tefact/feature-page";

export default {
  form, page, formDefault, pageDefault
} as Record<string, any>
