# Arrplat UI Library

### Run dev Server

```shell script
yarn run dev
```

### Build Change

```shell
yarn run build
```

### Add New Component

Create your file in `components` and add it in `index.ts` `components` and `export` block
